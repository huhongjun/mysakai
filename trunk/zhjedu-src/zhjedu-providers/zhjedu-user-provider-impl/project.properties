# Since these unit tests need Berkeley LDAP to be available over SSL 
# and require access to an Oracle DB, they should _not_ be run by 
# default. They can be run from the parent directory by specifying:
#
#    maven -Dmaven.test.skip=false cln bld

maven.test.skip=true
